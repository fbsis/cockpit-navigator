# Ace Editor runtime

Vendored from `ace-builds` version `1.44.0` (`src-min-noconflict`).

Only the editor core, diff and language-tools extensions, One Dark theme, and
the language modes used by Cockpit Navigator are included. Ace is distributed
under the BSD-3-Clause license in `LICENSE.txt`.
